#!/bin/bash

if [ $# -gt 2 ]; then
    echo $#
    echo "Invalid number of arguments."
    echo "Usage: db2bet <source_dir> <output_dir>"
else
    DEFAULT_PYTHON=`python --version`
    if [[ $DEFAULT_PYTHON != Python\ 2* ]]; then
        python /home1/03187/rstevens/STIR/pyscripts/make_bet_database.py $@
    else
        python /home1/03187/rstevens/STIR/pyscripts/make_bet_database.py $@
    fi
fi
