#!/bin/bash

if [ $# -gt 2 ]; then
    echo $#
    echo "Invalid number of arguments."
    echo "Usage: db_reg <source_dir> <output_dir>"
else
    STIRDB=/home1/03187/rstevens/STIR
    DEFAULT_PYTHON=`python --version`
    if [[ $DEFAULT_PYTHON != Python\ 2* ]]; then
        python $STIRDB/pyscripts/registration_new.py $@
    else
        python $STIRDB/pyscripts/registration_new.py $@
    fi
fi
